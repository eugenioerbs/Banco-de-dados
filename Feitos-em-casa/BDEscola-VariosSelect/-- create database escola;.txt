-- link chat: https://chatgpt.com/share/699cd10d-5260-8013-b6a9-456ce89f8975
-- create database escola;

use escola;

-/*create table escola 
(cd_escola int auto_increment primary key,
nm_escola varchar(50)
); -*/

-/*
create table aluno
(matri_aluno int primary key,
nm_aluno varchar(50),
cd_escola int,
constraint fk_escola
foreign key (cd_escola) references escola (cd_escola)
);
-*/

- /*
create table provasAluno
(matri_aluno int,
constraint fk_matri_aluno
foreign key (matri_aluno) references aluno (matri_aluno),
cd_prova int primary key
); -*/

-/*
create table amizades
(matri_aluno int,
matri_amigo int,
primary key (matri_aluno, matri_amigo),

constraint fk_aluno
foreign key (matri_aluno) references aluno (matri_aluno),

constraint fk_amigo
foreign key (matri_amigo) references aluno (matri_aluno)
); -*/

INSERT INTO escola (nm_escola) VALUES
('Escola Central'),
('Colégio Futuro'),
('Instituto Saber');

INSERT INTO aluno (matri_aluno, nm_aluno, cd_escola) VALUES
(1,'João',1),
(2,'Maria',1),
(3,'Pedro',1),
(4,'Julia',2),
(5,'Lucas',2),
(6,'Ana',3);

INSERT INTO provasAluno (matri_aluno, cd_prova) VALUES
(1,101),
(2,102),
(3,103),
(4,104),
(5,105),
(6,106);

INSERT INTO amizades (matri_aluno, matri_amigo) VALUES
(1,2), -- João amigo da Maria
(1,3), -- João amigo do Pedro
(2,4), -- Maria amiga da Julia
(3,5), -- Pedro amigo do Lucas
(4,5), -- Julia amiga do Lucas
(5,6); -- Lucas amigo da Ana

-- toda inserts feitas

desc escola;



-- 
select cd_escola, nm_aluno
from aluno;
-- 
select matri_aluno, nm_aluno, cd_escola
from aluno
where cd_escola > 1;
--
select matri_aluno, nm_aluno, cd_escola
from aluno
where cd_escola = 1;
--
select nm_escola, cd_escola
from escola
where cd_escola <> 1;
--
select * from aluno;

select nm_aluno, matri_aluno, cd_escola
from aluno
where matri_aluno = 1
and cd_escola = 1;
--
select nm_aluno, matri_aluno, cd_escola
from aluno
where matri_aluno = 1
or cd_escola = 1;
--
select * from aluno;

select *
from aluno
where nm_aluno
like '%a%';
--
select *
from aluno
where nm_aluno
like '%a%'
order by nm_aluno;
--
select *
from aluno
where nm_aluno like '%a%'
order by cd_escola;
--
select aluno.nm_aluno, escola.nm_escola
from aluno
join escola on aluno.cd_escola = escola.cd_escola;
--
select *
from provasaluno;

select aluno.nm_aluno, provasaluno.cd_prova
from aluno
join provasaluno on aluno.matri_aluno = provasaluno.matri_aluno;
--
select *
from escola;